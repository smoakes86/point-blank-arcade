[build]
builder = "nixpacks"

[build.nixpacks]
buildCmd = "cd ../.. && pnpm install && pnpm -r build"

[deploy]
startCommand = "node dist/index.js"
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
